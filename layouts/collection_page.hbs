{{> page_head }}
    <nav><a href="/">Home</a></nav>
    <h1>{{ title }}</h1>
    <ul>
    {{#each pages}}
      <li><a href="/{{ path }}">{{ title }}</a></li>
    {{/each}}
    </ul>
{{> page_foot }}
